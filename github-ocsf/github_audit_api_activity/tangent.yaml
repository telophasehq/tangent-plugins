runtime:
  plugins_path: "plugins/"
plugins:
  github_audit_api_activity:
    module_type: go
    path: .
    tests:
      - input: tests/input.json
        expected: tests/expected.json
sources:
  socket_main:
    type: socket
    path: "/tmp/sidecar.sock"
sinks:
  blackhole:
    type: blackhole
dag:
  - from:
      kind: source
      name: socket_main
    to:
      - kind: plugin
        name: github_audit_api_activity

  - from:
      kind: plugin
      name: github_audit_api_activity
    to:
      - kind: sink
        name: blackhole